export const getLoginDate = state => {
  return state.calendar.LoginDate;
};
export const getCurrentDate = state => {
  return state.calendar.currentDate;
};
export const getDayOrMonth = state => {
  return state.calendar.isDayOrMonth;
};
