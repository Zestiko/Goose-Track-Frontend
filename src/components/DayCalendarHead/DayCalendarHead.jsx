import PropTypes from 'prop-types';
import css from "./DayCalendarHead.module.css";

const DayCalendarHead = ({ id, children }) => {
    return (
      <>
      </>
    );
  };
  
  export default DayCalendarHead;
  
DayCalendarHead.propTypes = {
  };